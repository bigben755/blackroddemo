# SHA-256 hash of the shared sandbox password.
# Generate one with:
#   node -e "crypto.subtle.digest('SHA-256', new TextEncoder().encode('YOUR_PASSWORD')).then(b=>console.log(Buffer.from(b).toString('hex')))"
VITE_DEMO_PASSWORD_HASH=
